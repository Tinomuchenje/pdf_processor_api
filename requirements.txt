# Core Flask framework
Flask==3.0.3
Flask-Cors==5.0.0
Werkzeug==3.0.4

# API Documentation
flasgger==0.9.7.1

# PDF Processing
PyPDF2==3.0.1
pdf2image==1.17.0
pillow==10.4.0
pytesseract==0.3.13

# LLM Integration
requests==2.31.0
python-dotenv==1.0.0

# Essential Flask dependencies (auto-installed but pinned for stability)
blinker==1.8.2
click==8.1.7
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5

# Flasgger dependencies (auto-installed but pinned for stability)
jsonschema==4.23.0
PyYAML==6.0.2
